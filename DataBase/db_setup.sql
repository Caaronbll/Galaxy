CREATE DATABASE IF NOT EXISTS Planets;
USE Planets;

CREATE TABLE `Planets` (
  `Name` text,
  `radius `integer,
  `period` integer,
  `temp` integer,
  `distance` integer,
  `surface area` text,
  `lots` text,
  PRIMARY KEY (`Planets_index`)
);

